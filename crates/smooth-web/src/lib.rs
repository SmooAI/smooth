//! Smooth web dashboard — embedded Vite SPA served by axum.
//!
//! In production, the web assets are compiled into the binary via `rust-embed`.
//! In development, a Vite dev server runs separately.

// TODO: Phase 5 — embed web assets via rust-embed
// #[derive(RustEmbed)]
// #[folder = "web/dist/"]
// struct WebAssets;
