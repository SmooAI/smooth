{
    "name": "@smooth/auth",
    "version": "0.1.0",
    "private": true,
    "type": "module",
    "exports": {
        "./*": "./src/*"
    },
    "scripts": {
        "build": "tsup",
        "typecheck": "tsc --noEmit",
        "test": "vitest run"
    },
    "dependencies": {
        "@smooth/db": "workspace:*",
        "better-auth": "^1.5.0",
        "@better-auth/drizzle-adapter": "^1.5.0"
    },
    "devDependencies": {
        "tsup": "^8.4.0",
        "typescript": "^5.8.3",
        "vitest": "^3.1.0"
    }
}
