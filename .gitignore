# Environment (secrets)
.envrc.local
.env.local
.env*.local
.envrc.currentEnv

# Node (changesets only)
node_modules/

# Rust build
target/

# Turbo cache (leftover from TypeScript)
.turbo/

# Web build (Vite)
crates/smooth-web/web/dist/
crates/smooth-web/web/node_modules/

# Database
*.db
*.db-wal
*.db-shm

# Beads (data is gitignored, config is not)
.beads/dolt/
.beads/*.jsonl
.beads/embeddeddolt/

# IDE
.vscode/
.idea/

# OS
.DS_Store
Thumbs.db

# Logs
*.log

# Claude Code
.claude/worktree-bypass

# Beads / Dolt files
# Legacy per-project Dolt pearl stores (.smooth/dolt/) — pearls moved to
# ~/.smooth/pearls.db (pearl th-d3e842); the dir is only migration input
# and is never tracked. Entries go with the shim in pearl th-c6ba83.
/.dolt/
.smooth/dolt/
.beads-credential-key
.smooth/pearls.db
.smooth/pearls.db-wal
.smooth/pearls.db-shm
.archivist-diag.txt
.claude/worktrees/
