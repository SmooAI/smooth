source_env_if_exists .envrc.local

# Jira sync for Diver — env-var driven
export JIRA_URL="https://smooai.atlassian.net"
export JIRA_PROJECT="SMOODEV"
# JIRA_EMAIL and JIRA_API_TOKEN should be in .envrc.local (not committed)
