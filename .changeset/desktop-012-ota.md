---
'@smooai/smooth': patch
---

Desktop app → 0.1.2: OTA release carrying the daemon-addr discovery hardening (#373 — app never loads the wrong PWA) and the signed EventKit helper (#375 — Calendar/Reminders grants finally prompt on the Electron app). Cutting a new version so installed 0.1.1 clients auto-update.
