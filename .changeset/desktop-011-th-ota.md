---
'@smooai/smooth': patch
---

Desktop app → 0.1.1: first OTA release that bundles the `th` CLI (rides the update channel + PATH symlink from #363). Cutting a new version so installed 0.1.0 clients auto-update to the th-included build.
