---
'@smooai/smooth': patch
---

Desktop 0.1.4: OTA carrying the daemon self-refresh (#379 — smoo-hub stays on the relay instead of dropping off when its Smoo token expires) and reliable daemon auto-start + open-at-login (#380 — the app always runs its local daemon and starts at login, surviving reboot).
