---
'@smooai/smooth': patch
---

Big Smooth.app now ships a real Dock/Finder icon — the `th` PWA mark (dark rounded square, teal→blue glyph) as `scripts/macos/BigSmooth.icns`, wired via `CFBundleIconFile`. Previously the bundle declared no icon and got the generic macOS placeholder.
