---
'@smooai/smooth': patch
---

SmoothFlow 0.2.7. New Session has a real **Directory** field (th-145e6b). You can type to search every git repo and worktree under `~`, paste a path, or use **Browse…**. The pearl, branch and title follow whatever you pick. The daemon indexes `~` with fd's parallel walker and keeps the result in SQLite.
