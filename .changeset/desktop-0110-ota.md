---
'@smooai/smooth': patch
---

Desktop app OTA 0.1.10 — ships the session's merged work to the installed Big Smooth app: Plan⇄Auto modes + shift+tab / chip, Cmd+N new chat, the rich searchable model selector with capability badges, the fixed "Enable notifications" button + notify tool, and the safety-judge controls. Version bump so electron-updater serves it (it compares versions).
