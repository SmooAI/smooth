---
'@smooai/smooth': patch
---

SmoothFlow 0.2.6. The New Session picker flags a harness that launches but won't fully work: Codex with untrusted hooks, a stale plugin without the SmoothFlow hook, a missing login. It labels it "needs setup", explains why, and gives the fix command to copy (th-51bf88). Crash cards name the signal with macOS numbering (th-7be58a). A daemon that is merely slow no longer loses the hook address to a second daemon (th-4af55f).
